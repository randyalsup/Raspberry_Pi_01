[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200

; Serial upload (local development)
upload_port = COM3
upload_speed = 115200

; Libraries - none needed for simple UART bridge
; UWB library will be added when DWM3000 modules arrive
lib_deps =
